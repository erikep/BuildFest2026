# Railway deployment config for Tommie Shelf
# https://docs.railway.app/reference/config-as-code

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run deploy:start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
