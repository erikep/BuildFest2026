# Prisma — Person A (Database)

This directory is owned by **Person A**. Put here:

- `schema.prisma` — data models (Event, User, etc.)
- Migrations (after `prisma migrate dev`)
- Optional: `seed.ts` for sample data

Do not commit `*.db` or `*.db-journal` (see `.gitignore`).
